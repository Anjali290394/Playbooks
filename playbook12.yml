---
- name: Implementing host scope variables
  hosts: 172.31.12.102
  tasks:
    - name: Create user
      user:
        name: "{{a}}"
        password: "{{b}}"
        home: "{{c}}"
        comment: "{{d}}"
        uid: "{{e}}"
        shell: "{{f}}"
...
